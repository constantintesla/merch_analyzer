from __future__ import annotations

import json
from typing import Any


BEVERAGE_CLASSIFY_SYSTEM_PROMPT = """Ты система распознавания напитков на полке. На входе обычно один товар (бутылка/банка) или пустой/нечитаемый кроп.
Твоя задача: максимально точно определить бренд/тип и вернуть ОДНУ строку в строгом формате.

Ключевой принцип распознавания:
Проверяй товар по трем точкам в этом порядке:
1) Форма бутылки
2) Логотип/текстовый маркер на этикетке
3) Цвет жидкости/крышки/ключевых зон этикетки

Разрешенные линейки и визуальные якоря:

A) «Вкус Детства» (сладкая газировка)
- Форма: ребристая нижняя часть, заужение к середине, расширенные плечики.
- Этикетка: белый фон, сверху круглый логотип «Вкус Детства» (курсив).
- Вкусовые маркеры:
  - Лимонад: светло-желтый, лимоны и стакан.
  - Тархун: насыщенно-зеленый, пучок эстрагона.
  - Буратино: золотистый, красно-оранжевый логотип, силуэт персонажа.
  - Дюшес: темно-желтый, две крупные желтые груши.
  - Малина: розово-красный, паттерн ягод малины.

B) «ДЖОЙС» / «DJOIS» (современная газировка)
- Форма: строгий цилиндр, редкие горизонтальные желоба.
- Этикетка: геометрический паттерн треугольников, крупная надпись «ДЖОЙС».
- Вкусовые маркеры:
  - Cola: очень темная (почти черная) жидкость, красная этикетка, красная крышка, крупное слово «COLA» белыми буквами внизу этикетки.
    Допуск OCR: надпись бренда может читаться как «ДЖОЙС», «ДЖОИС», «DJOIS», «DJOIS».
  - Лимон: салатовая жидкость, зеленая этикетка, лимон в углу.
  - Апельсин: оранжевая жидкость и этикетка, долька апельсина.
  - Мохито: мутно-белый цвет, бирюзовая этикетка, лайм и мята.

C) «АСАРТИ»
- Форма: «песочные часы» с мелкозернистым рельефом на изгибах.
- Главный маркер: вертикальная красная полоса слева с надписью «АСАРТИ».
- Варианты вкусов: Кола (без сахара), Лимонад, Буратино, Апельсин, Ананас-Кокос, Лайм-Мята, Дюшес, Тархун, Ситро.

D) «Новотроицкая» (вода)
- Форма: широкие плечи, глубокие кольцевые желоба.
- Этикетка: пейзаж с хвойным лесом и горами.
- Типы:
  - Синяя этикетка/крышка: газированная.
  - Голубая (светло-синяя) этикетка/крышка: негазированная.
- КРИТИЧНО: не путай синий и голубой.
- Если оттенок неочевиден (блик/шум/тень), не угадывай тип: используй «Напиток: Новотроицкая» без типа.

E) «Пьютти» (вода)
- Форма: тонкая бутылка с ромбовидной насечкой («чешуя»).
- Этикетка: цветная горизонтальная полоса, в центре силуэт летящей белой птицы.
- Типы:
  - Фиолетовая полоса: газированная.
  - Голубая полоса: негазированная.
  - Красная полоса: минерализованная.

Фильтр «свой / чужой»:
- Если объект не проходит по форме+логотипу+маркерам разрешенных линеек, считай его чужим брендом.
- Маркеры чужого/неподходящего:
  1) другая характерная форма (например, талия Coca-Cola, винтовая Fanta и т.п.),
  2) нет надписей/логотипов: «Вкус Детства», «ДЖОЙС/DJOIS», «АСАРТИ», «Новотроицкая», «Пьютти»,
  3) конфликт ключевых маркеров (форма похожа, но нет обязательных элементов, например красной вертикальной полосы для «АСАРТИ»),
  4) крышка/этикетка явно противоречат типичному паттерну линейки.

Правила вывода (строго):
- Верни только одну строку без объяснений.
- Формат:
  - «Напиток: {название}» — если товар распознан,
  - «Ничего не обнаружено» — если товар не читается/отсутствует.
- Для чужого или непонятного бренда используй: «Напиток: Неизвестный бренд».
- Никогда не добавляй рассуждения, markdown, английские пояснения и служебный текст.

Примеры:
- Новотроицкая (синяя) -> Напиток: Новотроицкая газированная
- Новотроицкая (голубая) -> Напиток: Новотроицкая негазированная
- Пьютти (фиолетовая) -> Напиток: Пьютти газированная
- Пьютти (красная) -> Напиток: Пьютти минерализованная
- бутылка конкурента -> Напиток: Неизвестный бренд
- пустой кроп -> Ничего не обнаружено"""

BEVERAGE_RECHECK_USER_HINT = (
    "\n\nЭто повторная попытка: внимательно рассмотри этикетку, крышку и форму упаковки. "
    "Если бренд читается — укажи его в формате «Напиток: …». Ответь строго в том же формате, что в инструкции."
)


def build_classify_crop_user_prompt(*, retry: bool) -> str:
    base = (
        "Распознай напиток на изображении. Ответь строго в формате из системных инструкций. "
        "Только одна строка на русском: «Напиток: …» или «Ничего не обнаружено». "
        "Без английского текста и без рассуждений."
    )
    if retry:
        return base + BEVERAGE_RECHECK_USER_HINT
    return base


def build_batch_classify_user_intro(*, n: int) -> str:
    return (
        f"Перед тобой {n} изображений в порядке slot 0..{n - 1} "
        f"(первое изображение = slot 0, далее по порядку). "
        "Для каждого слота распознай напиток по правилам из системного промпта. "
        "Верни ТОЛЬКО один JSON без markdown и без текста вокруг. Формат:\n"
        '{"results":[{"slot":0,"line":"Напиток: … или Ничего не обнаружено"}, ...]}\n'
        f"Поле line — ровно одна строка на русском для этого слота. "
        f"В массиве results должны быть все слоты от 0 до {n - 1}."
    )


def build_recheck_item_name_prompt(*, item_name: str) -> str:
    return (
        "Проверь название товара и нормализуй его. "
        "КРИТИЧНО: не переводи на английский; если вход на русском (кириллица), ответ тоже на русском. "
        "Если название не похоже на товар или неуверенно, верни unknown. "
        "Верни только JSON с полями: "
        '{"item_name":"string","normalized_name":"string","confidence_raw":0.0,"status":"ok|uncertain|unknown"}. '
        f"Входное название: {item_name!r}"
    )


def build_planogram_from_full_image_prompt() -> str:
    return (
        "Ты анализируешь фото торговой полки (планограмма). "
        "Нужно перечислить ВСЕ видимые товарные позиции: порядок слева направо на каждой полке, "
        "полки считай сверху вниз (верхняя полка = shelf_id 1, следующая = 2, …). "
        "Не выдумывай товары: если не читается — кратко опиши что видно или unknown. "
        "КРИТИЧНО: название item_name на том же языке, что на упаковке (кириллица — без перевода на английский). "
        "Для КАЖДОЙ позиции обязательно укажи bbox_norm: прямоугольник вокруг товара в долях ширины/высоты кадра, "
        "ключи x1,y1,x2,y2, значения от 0 до 1 (левый верхний угол кадра = 0,0). "
        "Верни ТОЛЬКО один JSON-объект без markdown и без текста вокруг. Формат:\n"
        '{"shelves":[{"shelf_id":1,"items":['
        '{"position_in_shelf":1,"item_name":"строка","bbox_norm":{"x1":0.0,"y1":0.0,"x2":0.1,"y2":0.1},"confidence":0.8}'
        "]}]}\n"
        "Поле confidence — твоя уверенность в названии [0,1]. Если несколько одинаковых SKU подряд — отдельные позиции."
    )


def build_planogram_repair_prompt(*, content: str) -> str:
    return (
        "Преобразуй следующий текст в СТРОГИЙ JSON-объект формата:\n"
        '{"shelves":[{"shelf_id":1,"items":[{"position_in_shelf":1,"item_name":"string","bbox_norm":{"x1":0.0,"y1":0.0,"x2":0.1,"y2":0.1},"confidence":0.8}]}]}\n'
        "Только JSON, без markdown и без комментариев. "
        "Если данных не хватает, верни пустой объект с shelves: [].\n\n"
        f"Текст:\n{content}"
    )


def build_assess_placement_prompt(
    *,
    sku: str,
    reference_result: dict[str, Any],
    fact_result: dict[str, Any],
) -> str:
    return (
        "Ты сравниваешь эталон и факт выкладки одного SKU. "
        "Нужно вернуть, насколько плохо расставлено, в формате JSON. "
        "Верни ТОЛЬКО JSON с полями: "
        '{"badness_score":0,"verdict":"ok|minor|bad|critical","reason":"string","recommendation":"string"}.\n'
        "Шкала badness_score: 0=идеально, 100=очень плохо.\n"
        f"SKU: {sku}\n"
        f"Эталон: {json.dumps(reference_result, ensure_ascii=False)}\n"
        f"Факт: {json.dumps(fact_result, ensure_ascii=False)}"
    )
